name = "project-snapshot-ai"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# All sensitive values are set via wrangler secret put (encrypted in Cloudflare):
# CF_ACCOUNT_ID — your Cloudflare Account ID
# CF_API_TOKEN — your Cloudflare API Token with Workers AI permissions
